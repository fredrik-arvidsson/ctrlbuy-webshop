# =======================================
# E-POSTINSTÄLLNINGAR (UPPDATERAD)
# =======================================

# Spring Boot Mail konfiguration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=fredrik.g.arvidsson@gmail.com
spring.mail.password=cyfa knzz myvk naru
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.test-connection=false

# E-post applikationsinställningar
app.email.enabled=true
app.email.from=fredrik.g.arvidsson@gmail.com
app.email.from-name=CTRL+BUY Solutions
app.email.company-name=CtrlBuy

# Bas-URL för verifikationslänkar och bilder i e-post
app.base-url=http://localhost:8080

# Email Templates Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# =======================================
# JWT KONFIGURATION (SÄKERHET) - FIXED
# =======================================

# JWT Secret Key (bör vara minst 256 bitar för HS256)
jwt.secret=CtrlBuySecretKey2025VeryLongAndSecureKeyForJWTTokenGeneration123456789
jwt.expirationMs=86400000
jwt.refresh-token.expiration=604800000

# =======================================
# DATABAS KONFIGURATION
# =======================================

# H2 In-Memory Database för utveckling
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.h2.console.enabled=true

# =======================================
# LOGGING KONFIGURATION
# =======================================

# Debug logging för email och säkerhet
logging.level.com.ctrlbuy.webshop=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.security=DEBUG

# =======================================
# SERVER KONFIGURATION
# =======================================

# Server port (standard är 8080)
server.port=8080

# Session timeout (30 minuter)
server.servlet.session.timeout=30m

# =======================================
# UTVECKLING & TESTNING
# =======================================

# Aktivera H2 konsol för databas-debugging
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# Visa SQL queries i konsolen (endast för utveckling)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true