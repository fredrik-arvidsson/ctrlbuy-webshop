spring:
  datasource:
    url: jdbc:mysql://localhost:3306/webshop_test
    username: root
    password: infoTeeeeek25@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  test:
    database:
      replace: none
